# Quick Prompt — Micas互动培训课程

把我上传的资料改造成一套完整的 **Micas互动培训课程**，而不是逐页摘要、文档查看器、通用LMS或简单PPT。

必须满足：

- 以源文件为主要依据，完整读取正文、表格、图片、警告、步骤和附录，不得编造资料未支持的技术信息。
- 按学习目标重组为模块和短场景，每个场景只讲一个核心重点。
- 优先培训“必须操作、必须识别、必须理解”的内容；纯参考参数放进独立参考场景或搜索层。
- 使用源文件中的真实图片和示意图，裁掉页边和无关空白；可添加高亮、编号、箭头和局部放大，但不得改变设备事实。
- 每个场景提供独立语音稿，不要机械朗读页面文字。
- 加入真实工作场景题、模块测验、即时反馈和结业考试，答案必须映射到源文件。

## 一页一屏

- 整体必须像PPT一样前后翻页。
- 每个场景完整显示在一个浏览器视口内。
- 正常学习页面禁止上下滚动。
- Header、主内容区和底部控制栏共同限制在 `100dvh`。
- 内容放不下时拆成更多场景，不得缩小成难以阅读的小字。
- 分别在1920×1080、1600×900、1366×768以及四种语言下检查页面是否溢出。

## 视觉层级

不要把所有文字、卡片和图片都做成差不多大小、平均分布的死板页面。

每个场景必须先确定：

1. 一个最重要的核心信息；
2. 两到四个辅助信息；
3. 一个需要最大视觉空间的标题、产品图、关键数字、警告或操作步骤。

具体要求：

- 每页必须有一个明显的视觉焦点。
- Hero页和关键页的大标题要足够大，风格向原Preview版靠拢。
- 优先使用不对称的55/45、58/42或60/40布局。
- 一个主卡片可以明显大于其他辅助卡片。
- 不要大量重复使用所有卡片同样大小的2×2网格。
- 只有内容确实同等重要时才使用等宽等高卡片。
- 核心参数、限制、动作和警告必须被明显突出。

参考字号：

```css
.hero-title  { font-size: clamp(56px, 6vw, 96px); }
.scene-title { font-size: clamp(40px, 4.2vw, 68px); }
.key-metric  { font-size: clamp(52px, 5.5vw, 88px); }
```

## 图片要求

- 产品图片、安装图、结构图和截图默认必须完整显示，不能只显示一半。
- 技术图片使用 `object-fit: contain`，不得使用 `cover`裁切。
- 图片必须充分放大并利用页面空间，不能在大框里只放一个很小的图。
- 双栏技术页面中，图片通常至少占主舞台宽度的38%–45%。
- 图片本身是学习重点时，应占主舞台约50%–70%。
- 嵌入前先裁掉源文档页眉、页脚、边缘和无关白边。
- 图片仍然看不清时，拆成“完整图页面＋局部标注页面”，而不是强行缩小或截断。
- 只有作为明确局部放大图时才可裁切，并且相邻页面必须提供完整图。

## UI与品牌

- 整体视觉向深色Micas Preview版靠拢：深蓝背景、大标题、宽松留白、圆角蓝色/半透明卡片、精致的大尺寸产品图。
- 主色必须使用 **Micas蓝 `#00899F`**。
- Header、主舞台和底部控制区保持同一套深色视觉体系。
- Logo优先使用透明背景版本，禁止出现突兀的白色矩形底。
- 禁止主区域突然出现大面积纯白色面板。
- 完整课程目录默认隐藏，按钮位于左下角底部控制栏，通过抽屉打开。

## 语言按钮

- 默认语言：English。
- 支持：English、简体中文、繁體中文、日本語。
- 右上角下拉按钮只显示当前语言名称，例如 `English` 或 `简体中文`。
- 不要显示 `Language`、`Language · English`、`Language: English` 等前缀。
- 可以保留 `aria-label="Language"` 用于无障碍。
- 标题、正文、目录、旁白、题目、选项、反馈和结业信息均提供四种独立版本。

## 底部控制栏

- 左下角：课程目录和场景编号。
- 中部：更明显的进度条和前后翻页按钮。
- 右下角：Preview版风格的精简语音图标。
- 进度条建议宽度约300–480px（大屏），1366px宽度下尽量保持220–300px。
- 进度条高度建议8–12px，不要只是一条很细的装饰线。

## 语音

- 右下角只显示精简的扬声器/播放图标。
- 不显示语音名称、完整语音下拉框或大尺寸“语音讲解”按钮。
- 语速、字幕、试听和语音选择放进小型弹层。
- 每种语言最多显示3个高质量语音，并自动选择评分最高的同语言语音。
- 过滤低质量、旧式和重复语音，如eSpeak、Festival、MBROLA、Compact、Legacy、Robot、Desktop。

## 演示页面清洁度

正常课程页面不要出现：

- “源内容映射”或Source mapping；
- “旁白文本”或Narration transcript；
- QA标记、作者备注、调试信息和生成元数据。

这些信息保留在内部课程数据、Course Map和QA Report中。

## HTML功能

默认生成单个可离线打开的HTML，CSS、JavaScript和图片全部嵌入，不依赖外部CDN。包含：

- 一页一屏PPT式场景；
- 隐藏式课程目录抽屉；
- 四语言与English默认；
- 只显示当前语言名称的下拉按钮；
- 更大更清晰的进度条；
- 前后翻页与键盘翻页；
- 全屏和Video mode；
- 搜索；
- 图标式浏览器语音和精选语音设置；
- 自动演示；
- 答题反馈与结业评分；
- `localStorage`保存进度、语言和音频设置；
- 响应式移动端；
- 基础无障碍支持。

若资料存在矛盾、错误嫌疑或信息不足，不要自行修正，在QA Report中列出。

同时输出Course Map、README和QA Report。不要只给方案或样章，直接生成完整可下载成品。

课程设置：

- 课程名称：[填写]
- 受众：[填写]
- 时长：[填写]
- 培训目标：[填写]
- 考核难度：[基础/中级/认证级]