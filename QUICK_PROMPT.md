# Quick Prompt — Micas互动培训课程

请使用 **Micas Doc-to-Interactive-Training Skill**，把我上传的资料改造成一套完整的Micas互动培训课程，而不是逐页摘要、文档查看器、通用LMS或简单PPT。

## 内容与事实

- 以源文件为主要依据，完整读取正文、表格、图片、警告、步骤和附录。
- 不得编造资料未支持的技术信息。
- 按学习目标重组为模块和短场景，每个场景只讲一个核心重点。
- 优先培训“必须操作、必须识别、必须理解”的内容。
- 所有题目和答案必须能映射回源文件。

## 英文为视觉主版本

- 默认语言和视觉设计基准均为English。
- 先把英文页面做得最美观、最清晰、最有层级。
- 简体中文、繁體中文和日本語可以使用各自的换行、字号、比例或布局变体。
- 不要为了让四种语言页面完全一样，而削弱英文版的视觉效果。
- 四种语言都必须通过适配和功能检查。

## 一页一屏

- 每个场景必须在一个浏览器视口内完整显示。
- 正常学习页面禁止上下滚动。
- Header、内容区和底部控制区共同限制在`100dvh`内。
- 内容放不下时必须拆成更多场景，不能裁切、隐藏或把字体缩得很小。
- 必须在1920×1080、1600×900、1366×768和四种语言下检查。

## 严禁显示调试报错

最终页面禁止出现：

- `Layout overflow detected`
- overflow/fit warning
- QA标签
- debug信息
- source review required
- 缺失资源占位符

这些信息只能用于内部检查，不能显示给学员。只要任意场景溢出，就必须重新排版或拆页后再交付。

## 页面空间利用率

- 不允许出现一边有内容、另一边大片空白的低质量页面。
- 不允许保留空图片框、空列、空卡片或占位面板。
- 双栏布局必须两边都有有效教学内容。
- 如果没有合适图片，应改成有设计感的单栏布局，而不是留下半页空白。
- 页面要有一个明显视觉焦点，避免所有内容一样大、一样平。

## UI与品牌

- 主色使用Micas蓝`#00899F`。
- 整体采用深蓝统一背景、大标题、宽松留白、圆角蓝色/半透明卡片。
- 每页必须有一个最重要的标题、图片、数字、警告或动作。
- 避免每页都使用相同的等大小卡片网格。
- Logo使用适配深色背景的透明版本。
- 不允许大面积突兀白色主区域。

## 图片完整性

- 使用源文件中的真实图片和示意图。
- 先裁掉源文档页边、页眉页脚和无关白边。
- 技术图片必须使用`object-fit: contain`，不得使用`cover`裁切。
- 产品图、结构图、安装图和截图必须完整显示，不能只显示一半。
- 图片要充分放大利用页面空间，不能在大框里只放一张很小的图。
- 如果一页无法同时保证完整和清晰，先做完整图页面，再做局部放大页面。
- 缺图、半图或意外裁切都视为交付失败。

## 底部控件

- 课程目录、场景编号和进度条放在底部左侧。
- 底部右侧把4个按钮组成固定按钮组，顺序必须是：Previous → Next → 语音播放 → Audio settings。
- Previous和Next不要居中，也不要把语音按钮移到其他位置。
- 桌面端Previous和Next必须使用较大的文字按钮加箭头，不要默认只显示小箭头方块。
- 主按钮高度建议58–64px。
- 语音和设置图标按钮建议至少58×58px。
- 所有按钮禁止因为空间不足而缩得很小。
- 进度条应明显可见，桌面端建议宽度约340–520px，高度9–13px。

## 翻页逻辑

- 整门课程必须使用一条连续的全局页面序列。
- 只要后面还有页面，Next就必须可点击。
- 每个章节的最后一页必须直接进入下一章节的第一页。
- 模块测验和结业考试的每道题，即使尚未答题，Next也必须可点击。
- 未答题可以记录为未作答，允许之后返回补答，但不能阻塞翻页。
- 禁止使用`answered`、`submitted`、`passed`、得分、反馈或章节完成状态来禁用Next。
- 只有整门课程绝对最后的完成页可以禁用或替换Next。
- 键盘方向键、Video模式和页面按钮必须调用同一套翻页逻辑。

## 语言按钮

右上角语言按钮只显示当前语言：

- English
- 简体中文
- 繁體中文
- 日本語

不要显示`Language`前缀。无障碍属性中可以保留`aria-label="Language"`。

## 语音

- 正常页面右下角只显示简洁的语音播放图标和设置图标。
- 详细语音设置放进弹层，不要长期展示语音名称或完整列表。
- 每种语言最多显示3个高质量声音。
- 只要浏览器提供对应Google声音，就必须把Google声音排在第一位并设为默认。
- 优先级：Google精确语言 > Google同语言 > Microsoft Natural/Neural > Apple > 最佳同语言备选。
- 不得虚构浏览器不存在的Google声音；缺少时在QA报告中说明回退结果。

## 考试过渡页

正式结业考试前必须单独增加一页，说明：

- 教学内容已经完成；
- 接下来进入考试；
- 题目数量；
- 预计时长；
- 通过分数或评分方式；
- 作答方式；
- `Start Assessment`按钮。

自动播放模式必须在此页暂停等待，不能从最后一个教学页面突然直接跳到第一道考题。

## 页面清洁

普通学员页面禁止显示：

- 源内容映射；
- 旁白文本折叠区；
- 原始页码；
- 作者备注；
- QA和调试信息；
- 生成过程元数据。

这些内容放在Course Map和QA Report中。

## 输出与验收

默认生成：

1. 单文件离线HTML；
2. Course Map；
3. QA Report；
4. README。

交付前必须逐页检查：

- 无滚动和溢出；
- 无`Layout overflow detected`；
- 无半页空白或空容器；
- 所有图片完整且足够大；
- 所有桌面按钮尺寸正常；
- 所有非最终页面的Next都可用，包括章节末页和未答考试题；
- Previous、Next、语音播放、Audio settings按固定顺序靠右；
- 英文页面视觉效果最佳；
- 其他语言完整可用；
- Google声音可用时已作为默认；
- 考试前有单独过渡页；
- 离线资源、语音、导航、答题、评分和进度保存正常。

不要只给方案或样章，直接生成完整可下载成品，并在修复所有严重缺陷后再交付。

课程设置：

- 课程名称：[填写]
- 受众：[填写]
- 时长：[填写]
- 培训目标：[填写]
- 考核难度：[基础/中级/认证级]