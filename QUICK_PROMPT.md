# Quick Prompt — Micas互动培训课程

把我上传的资料改造成一套完整的 **Micas互动培训课程**，而不是逐页摘要、文档查看器或简单PPT。

必须满足：

- 以源文件为主要依据，完整读取正文、表格、图片、警告、步骤和附录；不得编造资料未支持的技术信息。
- 按学习目标重组为模块和短场景，每个场景只讲一个重点。
- 优先培训“必须操作、必须识别、必须理解”的内容；纯参考参数拆成独立参考场景或放进可搜索的参考层。
- 使用源文件中的真实图片和示意图，裁掉页边和无关内容；可添加高亮、编号、箭头和局部放大，但不得改变设备事实。
- 每个场景提供独立语音稿，不要机械朗读页面文字。
- 加入真实工作场景题、模块测验、即时反馈和结业考试，答案必须能映射到源文件。

## 一页一屏，禁止上下滚动

互动课程必须像PPT一样前后翻页：

- 每个场景完整显示在一个浏览器视口内。
- Header、正文舞台和底部控制栏合计不得超过 `100dvh`。
- 正常教学页面不允许出现纵向滚动条。
- Previous、Next、进度、课程目录和语音按钮必须始终可见。
- 内容放不下时，必须精简或拆成更多场景，不能继续向下堆叠。
- 不允许为了塞进一页而把文字缩得难以阅读。
- 至少在1920×1080、1600×900、1366×768、浏览器100%缩放下检查。
- English、简体中文、繁體中文、日本語四种语言都要分别检查是否溢出。
- 手机端也保持场景翻页模式，可用标签、弹层或增加场景，不能变成长网页。

## UI与品牌要求

- 整体视觉向“深色Micas预览版”靠拢：深蓝背景、大标题、宽松留白、圆角蓝色/半透明卡片、产品图精致框选。
- 主色必须使用 **Micas蓝 `#00899F`**。
- Header、主舞台和底部控制区必须保持同一套深色视觉体系。
- 不允许左上角出现与背景冲突的白色矩形Logo；优先使用透明背景Micas Logo。
- 不允许主区域突然出现占据大面积的纯白色面板；白色只能用于产品图片框、图表、表格等受控区域。
- 完整课程目录默认隐藏，通过抽屉式/弹层式目录打开。
- **课程目录按钮放在左下角底部控制栏，不放在左上角Header。**
- 正常教学、全屏和Video mode时，目录不得长期占用左侧空间。

## 演示页面清洁要求

正常学习和演示页面中不要显示：

- “源内容映射”或 `Source mapping`；
- “旁白文本”或 `Narration transcript`；
- 原始页码/章节映射折叠区；
- 作者备注、QA标签、调试信息和生成元数据。

这些信息应保留在内部数据、Course Map和QA Report中。旁白文本用于TTS；需要字幕时，从音频设置中按需打开，不要每页固定显示一大块旁白文本。

## 语言要求

- 默认语言：**English**。
- 必须支持：English、简体中文、繁體中文、日本語。
- 语言切换使用紧凑下拉按钮，按钮文字始终保持英文 **`Language`**。
- 正文、标题、模块名、旁白、题目、选项、反馈和结业信息均提供四种独立版本。
- 语音按 `en-US / zh-CN / zh-TW / ja-JP` 匹配，缺少系统语音时优雅降级。

## 语音按钮与音色筛选

- 右下角只显示一个类似Preview版的精简“扬声器/播放”图标按钮。
- 底部不要直接显示 `Samantha · en-US` 等语音名称。
- 底部不要放完整语音下拉框，也不要放很大的“语音讲解/Narrate”文字按钮。
- 点击图标开始、暂停或停止当前页面旁白。
- 详细设置通过图标打开小型弹层，包含语速、字幕、试听和语音选择。
- 每种语言最多只显示3个高质量语音，并自动选择评分最高的可用语音。
- 优先选择Natural/Neural/Premium/Enhanced，以及以下清晰语音系列：
  - English：Microsoft Aria/Jenny/Guy Online (Natural)、Apple Ava/Samantha、Google US English
  - 简体中文：Microsoft Xiaoxiao/Yunxi/Xiaoyi Online (Natural)、Google 普通话（中国大陆）、Apple Tingting
  - 繁體中文：Microsoft HsiaoChen/YunJhe Online (Natural)、Google 國語（臺灣）、Apple Mei-Jia
  - 日本語：Microsoft Nanami/Keita Online (Natural)、Google 日本語、Apple Kyoko/Otoya
- 过滤或大幅降权 `eSpeak`、`Festival`、`MBROLA`、`Compact`、`Legacy`、`Robot`、`Desktop` 等低质量/旧式语音。
- 不要把浏览器返回的几十个语音全部列出来；没有优质语音时，只显示1个最合适的同语言备选。

## HTML功能

默认生成单个可离线打开的HTML，CSS、JavaScript和图片全部嵌入，不依赖外部CDN。包含：

- `100dvh`固定演示舞台
- 正常场景无纵向滚动
- 左下角隐藏式课程目录抽屉入口
- 进度条和页码
- 前后翻页与键盘翻页
- 全屏
- 搜索
- 浏览器语音
- 右下角图标式语音控制
- 弹层式高质量语音选择
- 自动演示/Video mode
- 答题反馈与结业评分
- `localStorage`保存进度、语言和音频设置
- 响应式移动端
- 无障碍基础支持

若资料存在矛盾、错误嫌疑或信息不足，不要自行修正，在QA报告中列出。

同时输出课程地图、README和QA报告。源内容映射写进Course Map/QA Report，不要显示在每个演示页面。不要只给方案或样章，直接生成完整可下载成品。

课程设置：

- 课程名称：[填写]
- 受众：[填写]
- 时长：[填写]
- 培训目标：[填写]
- 考核难度：[基础/中级/认证级]
