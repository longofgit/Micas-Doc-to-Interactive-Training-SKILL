# Quick Prompt — Micas互动培训课程

把我上传的资料改造成一套完整的 **Micas互动培训课程**，而不是逐页摘要、文档查看器或简单PPT。

必须满足：

- 以源文件为主要依据，完整读取正文、表格、图片、警告、步骤和附录；不得编造资料未支持的技术信息。
- 按学习目标重组为模块和短场景，每个场景只讲一个重点。
- 优先培训“必须操作、必须识别、必须理解”的内容；纯参考参数放进可搜索/可展开的参考页面。
- 使用源文件中的真实图片和示意图，裁掉页边和无关内容；可添加高亮、编号、箭头和局部放大，但不得改变设备事实。
- 每个场景提供独立语音稿，不要机械朗读页面文字。
- 加入真实工作场景题、模块测验、即时反馈和结业考试，答案必须能映射到源文件。

## UI与品牌要求

- 整体视觉向“深色Micas预览版”靠拢：深蓝背景、大标题、宽松留白、圆角蓝色/半透明卡片、产品图精致框选。
- 主色必须使用 **Micas蓝 `#00899F`**。
- Header、主舞台和底部控制区必须保持同一套深色视觉体系。
- 不允许左上角出现与背景冲突的白色矩形Logo；优先使用透明背景Micas Logo。
- 不允许主区域突然出现占据大面积的纯白色面板；白色只能用于产品图片框、图表、表格等受控区域。
- 完整课程目录默认隐藏，通过顶部按钮打开抽屉式/弹层式目录；正常教学、全屏和Video mode时不长期占用左侧空间。

## 语言要求

- 默认语言：**English**。
- 必须支持：English、简体中文、繁體中文、日本語。
- 语言切换使用紧凑下拉按钮，按钮文字始终保持英文 **`Language`**。
- 正文、标题、模块名、旁白、题目、选项、反馈和结业信息均提供四种独立版本。
- 语音按 `en-US / zh-CN / zh-TW / ja-JP` 匹配，缺少系统语音时优雅降级。

## HTML功能

默认生成单个可离线打开的HTML，CSS、JavaScript和图片全部嵌入，不依赖外部CDN。包含：

- 隐藏式课程目录抽屉
- 进度条和页码
- 前后翻页与键盘翻页
- 全屏
- 搜索
- 浏览器语音与语音选择
- 自动演示/Video mode
- 答题反馈与结业评分
- `localStorage`保存进度、语言和音频设置
- 响应式移动端
- 无障碍基础支持

若资料存在矛盾、错误嫌疑或信息不足，不要自行修正，在QA报告中列出。

同时输出课程地图、源内容映射、README和QA报告。不要只给方案或样章，直接生成完整可下载成品。

课程设置：

- 课程名称：[填写]
- 受众：[填写]
- 时长：[填写]
- 培训目标：[填写]
- 考核难度：[基础/中级/认证级]
